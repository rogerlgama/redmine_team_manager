# frozen_string_literal: true

# Kept as a compatibility placeholder. The project overview enhancement is
# rendered through a Redmine view hook so the native role grouping remains
# untouched.
module RedmineTeamManager
  module Patches
    module ProjectsControllerPatch
    end
  end
end
